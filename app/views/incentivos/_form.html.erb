<%= form_for(@incentivo) do |f| %>
  <% if @incentivo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@incentivo.errors.count, "error") %> prohibited this incentivo from being saved:</h2>

      <ul>
      <% @incentivo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :integer %><br>
    <%= f.text_field :integer %>
  </div>
  <div class="field">
    <%= f.label :integer %><br>
    <%= f.text_field :integer %>
  </div>
  <div class="field">
    <%= f.label :decimal %><br>
    <%= f.text_field :decimal %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
